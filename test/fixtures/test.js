'use strict';

var expect = require('chai').expect;

describe('The fixtures module', function() {

  describe('The config module', function() {

    it('should copy the files in the config folder', function(done) {
      done();
    });



  });


});
