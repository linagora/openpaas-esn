div(ng-controller='currentDomainController')
  activity-stream(activitystream-uuid='{{domain.activity_stream.uuid}}')
  info-notification
    activitystream-notification(room='{{domain.activity_stream.uuid}}')
  confirm-notification
    conference-notification
