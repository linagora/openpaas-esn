.toggle-switch(data-ts-color="{{color}}")
  label.ts-label(ng-if="label") {{label}}
  input(type="checkbox", hidden, ng-checked="ngModel")
  label.ts-helper(ng-click="toggle(); $event.stopPropagation();")
