md-menu.blue(md-position-mode='right bottom')
  img.header-avatar(ng-src="{{ctrl.avatarURL}}", alt=__("Avatar"), aria-label='menu', ng-click='ctrl.openMenu($mdMenu, $event)')
  md-menu-content.dm-icon.profile-menu
    md-menu-item
      md-button(ui-sref="profile({ user_id: '' })")
        md-icon(md-svg-icon="account",aria-label="Account Icon")
        | #{__('Profile')}
    div(dynamic-directive="profile-menu-link")
    md-menu-item
      md-button(ui-sref="logout")
        md-icon(md-svg-icon="power",aria-label="power Icon")
        | #{__('Logout')}
