md-select.md-no-underline(ng-model="ctrl.selectedProvider", ng-change="ctrl.onProviderSelected({provider: ctrl.selectedProvider})", aria-label=__('Search type'), title=__('Search type'))
  md-option.option-all(ng-value="all" ng-selected="{{ ctrl.selectAll }}") #{__('All')}
  md-option(ng-value="provider", ng-repeat="provider in ctrl.providers", ng-selected="{{ provider.active }}")
    img.option-icon(ng-if="provider.icon", ng-src="{{ provider.icon }}")
    | {{ provider.name | esnI18n }}