.contacts.clearfix.row.fadeIn200ms
  div(data-contact-index="{{letter}}" ng-repeat="(letter, contacts) in sorted_contacts" on-finish-render)
    contact-list-card.col-md-4.col-sm-6.col-xs-12(contact='contact', ng-repeat="contact in contacts")
