.subscription-item
  .meta.ellipsis
    esn-avatar.attendee-avatar(user-id="$ctrl.addressbook.bookId")
    .title.ellipsis
      .name
        span {{::$ctrl.addressbookDisplayName}}
      .owner
        span {{::$ctrl.addressbook.user.displayName}}
  .rights
    //implement right display
  .actions
    toggle-switch(ng-model='$ctrl.addressbook.isSelected')
