ng-form(name="form")
  .card
    .card-header
      h2 #{__('Date time')}
        small #{__('Change your preferred date and time format')}
    .card-body.card-padding
      esn-datetime-format-selector(date-format="$ctrl.datetime.dateFormat", time-format="$ctrl.datetime.timeFormat")
