div
  attendee-list-item-edition(ng-if="mode === 'edition'", attendee='attendee', is-organizer='isOrganizer', read-only='::readOnly')
  attendee-list-item-consult(ng-if="mode === 'consult'", attendee='attendee', is-organizer='isOrganizer', read-only='::readOnly')
