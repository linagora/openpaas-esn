sub-header
  inbox-configuration-folder-subheader

.inbox-configuration-folders
  .mailboxes-list
    mailbox-display.lv-item(mailbox="mailbox", ng-repeat="mailbox in mailboxes | filter: { role: '!'}", ui-sref="unifiedinbox.configuration.folders.folder({ mailbox: mailbox.id})")

fab(ui-sref='unifiedinbox.configuration.folders.add')