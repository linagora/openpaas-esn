.clickable(title="{{ ::mailbox.name }}")
  .badge.pull-right(ng-hide='mailbox.unreadMessages === 0') {{ mailbox.unreadMessages }}
  i.pull-left.inbox-icon(ng-class='mailboxIcons')
  .mailbox-name
    span.mailbox-name-inner
      bdi {{ ::mailbox.qualifiedName }}
    span.mailbox-name-after
