textarea.compose-body(auto-size-dynamic="!isBoxed()", ng-model="email.textBody", ng-class="{ 'isCollapsed': isCollapsed }", placeholder="#{__('Start writing your email here')}")
.edit-quoted-mail(ng-if='email.quoted && !email.isQuoting')
  a(href='', ng-click='editQuotedMail()') #{__('Edit quoted mail')}
