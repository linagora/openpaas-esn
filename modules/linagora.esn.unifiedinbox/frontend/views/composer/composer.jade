sub-header
  composer-subheader

form.form-horizontal.compose.ng-cloak

  .compose-header
    .fields
      .form-group
        label.control-label #{__('To')}
        recipients-auto-complete.inbox-recipients(ng-click='openRecipients("to")', ng-model="email.to", template="fullscreen-recipients-auto-complete")
      .form-group(ng-if="!isCollapsed")
        label.control-label #{__('CC')}
        recipients-auto-complete.inbox-recipients(ng-click='openRecipients("cc")', ng-model="email.cc", template="fullscreen-recipients-auto-complete")
      .form-group(ng-if="!isCollapsed")
        label.control-label #{__('BCC')}
        recipients-auto-complete.inbox-recipients(ng-click='openRecipients("bcc")', ng-model="email.bcc", template="fullscreen-recipients-auto-complete")
    .inbox-more-recipients(ng-click="isCollapsed = !isCollapsed", ng-class="{ 'isCollapsed': isCollapsed }")
      i.mdi(ng-class="isCollapsed ? 'mdi-chevron-down' : 'mdi-chevron-up'")

  input.form-control.compose-subject(type="text", placeholder="#{__('Subject')}", ng-model="email.subject")

  email-body-editor
  composer-attachments
